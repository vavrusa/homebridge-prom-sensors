/**
 * Plugin name - must match the "name" in package.json
 */
export const PLUGIN_NAME = 'homebridge-prom-sensors';

/**
 * Platform name - used in config.json
 */
export const PLATFORM_NAME = 'PrometheusMetrics';
